# Raw Sources

현재 원본 PDF는 저장소 루트에 보관되어 있으며, 중복 저장은 아직 하지 않는다.

기준 문서:

- `2025학년도 2022 개정 교육과정 선택 과목 안내서 (1).pdf`

권장 후속 작업:

1. 원본 PDF를 이 디렉터리로 복사하거나 심볼릭 링크 정책을 정한다.
2. `pdftotext` 결과를 `.txt`로 저장해 파서 입력 정본을 만든다.
3. 문서 해시(`sha256`)를 계산해 `curriculum-sources.json`의 `checksum`에 반영한다.
